# Cert issuance via Let's Encrypt, validated by DNS-01 against Cloudflare.
# CLOUDFLARE_API_TOKEN must have Zone:DNS:Edit permission on the gdlf zone.
issuer acme {
    dns cloudflare {env.CLOUDFLARE_API_TOKEN}
}
